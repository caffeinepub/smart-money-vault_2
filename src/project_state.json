{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 29,
  "summary": "Update SubscriptionPanel.tsx from hardcoded tier cards to dynamic tiers loaded via useListTiers with graceful fallback to existing hardcoded tiers; wire Stripe checkout via useCreateCheckoutSession and gate Subscribe buttons based on useIsStripeConfigured, adding per-tier Subscribe/Current Plan CTA while preserving existing glassmorphism styling and Popular badge.",
  "architectural_notes": [
    "Stripe integration must not modify backend/stripe/stripe.mo; actor should store optional stable StripeConfiguration and call Stripe.createCheckoutSession/getSessionStatus using existing transform function.",
    "Expose Stripe-related types (ShoppingItem, StripeSessionStatus) as public types from the actor for frontend consumption.",
    "Project context: \"Smart Money Vault\" (institutional trading command center on ICP) with single Motoko canister backend/main.mo + React frontend + off-chain Python trading bot.",
    "Constraints: preserve ALL legacy public methods listed in backend/main.mo header; all mutations must be update calls (never query); keep files <1000 lines (split into modules if needed); do not modify stable/tested modules unless explicitly told; do not modify frontend paths: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui/*.",
    "Access control rules: use AccessControl.hasPermission for user checks and AccessControl.isAdmin for admin checks.",
    "Cycle balance API constraint: Runtime.cyclesBalance() is not available in the current Motoko version; use ExperimentalCycles.balance() if available, otherwise set cycles to 0 and add comment `// TODO: Replace with actual cycle balance API when available.`",
    "For `check_uplink` performance and correctness, maintain a stable `botIdIndex` (bot_id → owner Principal) updated in `saveCallerUserProfile`, and use it instead of scanning all `userProfiles`. Enforce non-empty signature with a trap and include a Phase 2 TODO for full Ed25519 verification.",
    "Connect Bot Wizard must generate a real Ed25519 keypair (TweetNaCl preferred) so the registered publicKey corresponds to the user’s private key; avoid unrelated random byte arrays.",
    "Ed25519 key generation constraint update: TweetNaCl and WebCrypto Ed25519 are not available; Connect Bot Wizard must use `@noble/ed25519` as primary. If noble is also unavailable, fall back to a 32-byte random seed and include comment `// TODO: Replace with real Ed25519 derivation in production.`; do not change any other wizard behavior.",
    "Build/deploy scope clarified: retry only the last failed deploy step (backend `getHeartbeatData` / `fetchSignals` changes), not the full backend+frontend+Stripe+keypair set."
  ],
  "known_issues": [
    "Repeated build attempts for the getHeartbeatData change failed with the assistant reporting it 'couldn't implement that request'; root cause not identified in conversation.",
    "Motoko version lacks Runtime.cyclesBalance(); heartbeat cycles must use ExperimentalCycles.balance() or fall back to 0 with a TODO comment.",
    "Attempt to implement the `fetchSignals` structured HTTP response return and type change failed (assistant reported inability to implement); retry requested with smaller substeps.",
    "Environment constraint: TweetNaCl and WebCrypto Ed25519 are unavailable; keypair generation must switch to `@noble/ed25519` or use the specified random-seed placeholder fallback.",
    "Phase B gate check found ConnectBotWizardModal keypair generation still using WebCrypto Ed25519; must switch to `@noble/ed25519` primary with specified fallback + TODO comment per REQ-24 before Phase C."
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-update-the-dashboard-heartbeat-ui-to-tolerate-and-display-the-new-optional-cycleswarning-field-returned-by-the-backend-heartbeat-api",
      "title": "Update the dashboard heartbeat UI to tolerate and display the new optional `cyclesWarning` field returned by the backend heartbeat API.",
      "reqIds": [
        "REQ-8"
      ],
      "version": 1
    },
    {
      "id": "feature-update-the-frontend-connectivity-test-hook-frontend-src-hooks-usefetchsignalstest-ts-to-handle-the-new-fetchsignals-success-shape-statuscode-body-timestamp-and-update-the-success-toast-message-to-reflect-the-structured-result-e-g-include-status-code-and-or-body-length-rather-than-signals-length",
      "title": "Update the frontend connectivity-test hook `frontend/src/hooks/useFetchSignalsTest.ts` to handle the new `fetchSignals` success shape (`{ statusCode; body; timestamp }`) and update the success toast message to reflect the structured result (e.g., include status code and/or body length) rather than `signals.length`.",
      "reqIds": [
        "REQ-18"
      ],
      "version": 1
    },
    {
      "id": "feature-regenerate-or-update-the-frontend-s-backend-interface-type-definitions-e-g-the-generated-declarations-used-by-the-actor-so-typescript-correctly-reflects-the-updated-signalfetchresult-candid-type-and-actor-fetchsignals-return-type",
      "title": "Regenerate or update the frontend’s backend interface type definitions (e.g., the generated declarations used by the actor) so TypeScript correctly reflects the updated `SignalFetchResult` Candid type and `actor.fetchSignals()` return type.",
      "reqIds": [
        "REQ-19"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-settings-connectbotwizardmodal-tsx-so-the-connect-bot-wizard-generates-a-real-ed25519-keypair-where-the-public-key-matches-the-private-key-using-tweetnacl-tweetnacl-if-available-if-tweetnacl-is-not-available-use-noble-ed25519-instead-and-if-dependency-installation-is-not-possible-fall-back-to-web-crypto-api-ed25519-key-generation",
      "title": "Update `frontend/src/components/settings/ConnectBotWizardModal.tsx` so the Connect Bot Wizard generates a real Ed25519 keypair (where the public key matches the private key) using TweetNaCl (`tweetnacl`) if available; if TweetNaCl is not available, use `@noble/ed25519` instead, and if dependency installation is not possible, fall back to Web Crypto API Ed25519 key generation.",
      "reqIds": [
        "REQ-21"
      ],
      "version": 1
    },
    {
      "id": "feature-replace-the-existing-handlegeneratekeypair-implementation-in-frontend-src-components-settings-connectbotwizardmodal-tsx-with-the-ed25519-key-generation-flow-specified-by-the-user-generate-the-keypair-display-the-private-key-as-a-hex-string-bot-private-key-and-keep-the-public-key-as-a-uint8array-for-sending-to-the-backend-keep-all-other-wizard-steps-ui-behaviors-blur-copy-and-the-existing-linkmutation-call-flow-unchanged",
      "title": "Replace the existing `handleGenerateKeypair` implementation in `frontend/src/components/settings/ConnectBotWizardModal.tsx` with the Ed25519 key generation flow specified by the user: generate the keypair, display the private key as a hex string (BOT_PRIVATE_KEY), and keep the public key as a `Uint8Array` for sending to the backend; keep all other wizard steps, UI behaviors (blur/copy), and the existing `linkMutation` call flow unchanged.",
      "reqIds": [
        "REQ-22"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-settings-connectbotwizardmodal-tsx-handlegeneratekeypair-so-it-no-longer-uses-tweetnacl-or-web-crypto-ed25519-instead-generate-the-keypair-using-noble-ed25519-exactly-as-follows-import-as-ed-from-noble-ed25519-const-privatekey-ed-utils-randomprivatekey-const-publickey-await-ed-getpublickeyasync-privatekey-display-the-private-key-as-a-hex-string-for-bot-private-key-and-keep-the-public-key-as-a-uint8array-for-sending-to-the-backend-if-noble-ed25519-is-not-available-at-runtime-build-time-fall-back-to-generating-a-32-byte-random-seed-and-produce-a-placeholder-derived-public-key-including-the-exact-comment-todo-replace-with-real-ed25519-derivation-in-production-do-not-change-any-other-ui-steps-blur-copy-behavior-or-the-existing-linkmutation-call-flow",
      "title": "Update `frontend/src/components/settings/ConnectBotWizardModal.tsx` `handleGenerateKeypair` so it no longer uses TweetNaCl or Web Crypto Ed25519. Instead, generate the keypair using `@noble/ed25519` exactly as follows: `import * as ed from '@noble/ed25519'; const privateKey = ed.utils.randomPrivateKey(); const publicKey = await ed.getPublicKeyAsync(privateKey);`. Display the private key as a hex string (for BOT_PRIVATE_KEY) and keep the public key as a `Uint8Array` for sending to the backend. If `@noble/ed25519` is not available at runtime/build-time, fall back to generating a 32-byte random seed and produce a placeholder derived public key, including the exact comment: `// TODO: Replace with real Ed25519 derivation in production.` Do not change any other UI, steps, blur/copy behavior, or the existing `linkMutation` call flow.",
      "reqIds": [
        "REQ-24"
      ],
      "version": 1
    },
    {
      "id": "feature-refactor-frontend-src-components-layout-appshell-tsx-to-replace-the-current-usestate-screen-toggle-with-a-proper-route-system-hash-routing-or-simple-state-derived-from-window-location-supporting-these-routes-dashboard-default-trades-overview-settings-admin-payment-success-payment-failure",
      "title": "Refactor `frontend/src/components/layout/AppShell.tsx` to replace the current `useState` screen toggle with a proper route system (hash routing or simple state derived from `window.location`), supporting these routes: `dashboard` (default), `trades`, `overview`, `settings`, `admin`, `payment-success`, `payment-failure`.",
      "reqIds": [
        "REQ-28"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-dashboard-dashboardheader-tsx-navigation-to-include-links-for-dashboard-default-trades-overview-settings-and-admin-admin-only-admin-visibility-must-be-gated-by-useiscalleradmin-from-frontend-src-hooks-useadmin-ts",
      "title": "Update `frontend/src/components/dashboard/DashboardHeader.tsx` navigation to include links for: Dashboard (default), Trades, Overview, Settings, and Admin (admin-only). Admin visibility must be gated by `useIsCallerAdmin` from `frontend/src/hooks/useAdmin.ts`.",
      "reqIds": [
        "REQ-29"
      ],
      "version": 1
    },
    {
      "id": "feature-in-frontend-src-components-layout-appshell-tsx-render-the-correct-screen-component-based-on-the-current-route-using-these-mappings-dashboard-dashboardscreen-trades-tradesscreen-overview-overviewscreen-settings-merged-settingsscreen-admin-adminscreen-admin-only-payment-success-paymentsuccessscreen-payment-failure-paymentfailurescreen-import-all-referenced-screens-and-useiscalleradmin-for-admin-gating",
      "title": "In `frontend/src/components/layout/AppShell.tsx`, render the correct screen component based on the current route, using these mappings: `dashboard` → `DashboardScreen`, `trades` → `TradesScreen`, `overview` → `OverviewScreen`, `settings` → merged `SettingsScreen`, `admin` → `AdminScreen` (admin-only), `payment-success` → `PaymentSuccessScreen`, `payment-failure` → `PaymentFailureScreen`. Import all referenced screens and `useIsCallerAdmin` for admin gating.",
      "reqIds": [
        "REQ-30"
      ],
      "version": 1
    },
    {
      "id": "feature-merge-frontend-src-screens-settingsroutescreen-tsx-into-frontend-src-screens-settingsscreen-tsx-by-updating-settingsscreen-to-provide-4-tabs-profile-renders-profilesettingsform-connect-bot-renders-connectbotpanel-subscription-renders-subscriptionpanel-and-is-visible-to-all-users-and-admin-tiers-renders-admintiermanagementpanel-and-is-visible-only-for-admins-using-useiscalleradmin",
      "title": "Merge `frontend/src/screens/SettingsRouteScreen.tsx` into `frontend/src/screens/SettingsScreen.tsx` by updating `SettingsScreen` to provide 4 tabs: \"Profile\" (renders `ProfileSettingsForm`), \"Connect Bot\" (renders `ConnectBotPanel`), \"Subscription\" (renders `SubscriptionPanel` and is visible to all users), and \"Admin Tiers\" (renders `AdminTierManagementPanel` and is visible only for admins using `useIsCallerAdmin`).",
      "reqIds": [
        "REQ-31"
      ],
      "version": 1
    },
    {
      "id": "feature-delete-frontend-src-screens-settingsroutescreen-tsx-after-its-content-is-merged-it-is-currently-an-empty-placeholder-ensure-imports-usages-are-removed-so-the-build-passes",
      "title": "Delete `frontend/src/screens/SettingsRouteScreen.tsx` after its content is merged (it is currently an empty placeholder). Ensure imports/usages are removed so the build passes.",
      "reqIds": [
        "REQ-32"
      ],
      "version": 1
    },
    {
      "id": "feature-remove-the-support-related-phrases-community-support-priority-support-and-dedicated-support-so-they-no-longer-appear-anywhere-in-the-subscription-tab-ui-including-the-tier-features-list-rendering",
      "title": "Remove the support-related phrases \"Community support\", \"Priority Support\", and \"Dedicated Support\" so they no longer appear anywhere in the Subscription tab UI (including the tier features list rendering).",
      "reqIds": [
        "REQ-33"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-settings-subscriptionpanel-tsx-to-wire-the-existing-subscription-stripe-hooks-into-the-existing-tier-card-ui-so-it-is-interactive-clicking-a-tier-card-initiates-checkout-immediately-for-paid-tiers-and-the-user-s-currently-active-tier-is-visually-highlighted",
      "title": "Update `frontend/src/components/settings/SubscriptionPanel.tsx` to wire the existing subscription/Stripe hooks into the existing tier-card UI so it is interactive: clicking a tier card initiates checkout immediately for paid tiers, and the user’s currently active tier is visually highlighted.",
      "reqIds": [
        "REQ-34"
      ],
      "version": 1
    },
    {
      "id": "feature-make-the-subscription-tier-card-layout-responsive-across-all-screen-sizes-with-tier-cards-stacking-vertically-on-small-screens",
      "title": "Make the Subscription tier-card layout responsive across all screen sizes, with tier cards stacking vertically on small screens.",
      "reqIds": [
        "REQ-35"
      ],
      "version": 1
    },
    {
      "id": "feature-optimize-the-entire-app-ui-for-all-screen-sizes-by-auditing-and-adjusting-layouts-across-major-screens-components-dashboard-trades-overview-settings-admin-and-payment-result-screens-to-prevent-overflow-cramped-spacing-and-unreadable-typography-at-common-breakpoints",
      "title": "Optimize the entire app UI for all screen sizes by auditing and adjusting layouts across major screens/components (Dashboard, Trades, Overview, Settings, Admin, and payment result screens) to prevent overflow, cramped spacing, and unreadable typography at common breakpoints.",
      "reqIds": [
        "REQ-36"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-settings-subscriptionpanel-tsx-to-import-and-use-these-hooks-exactly-as-specified-by-the-user-uselisttiers-from-hooks-usesubscriptiontiers-usecreatecheckoutsession-from-hooks-usestripecheckout-and-useisstripeconfigured-from-hooks-usestripestatus",
      "title": "Update `frontend/src/components/settings/SubscriptionPanel.tsx` to import and use these hooks exactly as specified by the user: `useListTiers` from `hooks/useSubscriptionTiers`, `useCreateCheckoutSession` from `hooks/useStripeCheckout`, and `useIsStripeConfigured` from `hooks/useStripeStatus`.",
      "reqIds": [
        "REQ-37"
      ],
      "version": 1
    },
    {
      "id": "feature-replace-the-hardcoded-tier-card-html-in-frontend-src-components-settings-subscriptionpanel-tsx-with-tier-data-loaded-from-uselisttiers-while-tiers-are-loading-show-a-loading-spinner-if-uselisttiers-returns-an-error-or-returns-an-empty-list-fall-back-to-the-existing-hardcoded-tiers-graceful-degradation",
      "title": "Replace the hardcoded tier-card HTML in `frontend/src/components/settings/SubscriptionPanel.tsx` with tier data loaded from `useListTiers()`. While tiers are loading, show a loading spinner. If `useListTiers()` returns an error or returns an empty list, fall back to the existing hardcoded tiers (graceful degradation).",
      "reqIds": [
        "REQ-38"
      ],
      "version": 1
    },
    {
      "id": "feature-add-a-per-tier-cta-in-frontend-src-components-settings-subscriptionpanel-tsx-for-each-paid-tier-render-a-subscribe-button-that-initiates-stripe-checkout-for-the-free-tier-render-current-plan-instead-of-a-button-clicking-subscribe-must-call-createcheckoutsession-with-a-shoppingitem-containing-the-tier-s-name-description-priceincents-and-quantity-1-on-success-redirect-to-session-url-via-window-location-href-while-the-checkout-mutation-is-pending-show-a-loading-spinner-on-that-tier-s-button",
      "title": "Add a per-tier CTA in `frontend/src/components/settings/SubscriptionPanel.tsx`: for each paid tier, render a \"Subscribe\" button that initiates Stripe checkout; for the free tier, render \"Current Plan\" instead of a button. Clicking \"Subscribe\" must call `createCheckoutSession` with a `ShoppingItem` containing the tier’s `name`, `description`, `priceInCents`, and `quantity` 1; on success redirect to `session.url` via `window.location.href`. While the checkout mutation is pending, show a loading spinner on that tier’s button.",
      "reqIds": [
        "REQ-39"
      ],
      "version": 1
    },
    {
      "id": "feature-gate-paid-tier-subscribing-in-frontend-src-components-settings-subscriptionpanel-tsx-using-useisstripeconfigured-when-stripe-is-not-configured-disable-all-subscribe-buttons-and-show-the-message-below-the-cards-payment-processing-is-being-configured-check-back-soon-preserve-the-existing-glassmorphism-tier-card-styling-and-the-popular-badge",
      "title": "Gate paid-tier subscribing in `frontend/src/components/settings/SubscriptionPanel.tsx` using `useIsStripeConfigured`: when Stripe is not configured, disable all \"Subscribe\" buttons and show the message below the cards: \"Payment processing is being configured. Check back soon.\" Preserve the existing glassmorphism tier-card styling and the \"Popular\" badge.",
      "reqIds": [
        "REQ-40"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-settings-connectbotwizardmodal-tsx-so-handlegeneratekeypair-no-longer-uses-web-crypto-ed25519-and-instead-generates-the-ed25519-keypair-using-noble-ed25519-exactly-as-follows-import-as-ed-from-noble-ed25519-const-privatekey-ed-utils-randomprivatekey-const-publickey-await-ed-getpublickeyasync-privatekey-display-the-private-key-as-a-hex-string-bot-private-key-and-keep-the-public-key-as-a-uint8array-for-sending-to-the-backend-if-noble-ed25519-is-not-available-at-runtime-build-time-fall-back-to-generating-a-32-byte-random-seed-and-produce-a-placeholder-derived-public-key-including-the-exact-comment-todo-replace-with-real-ed25519-derivation-in-production-do-not-change-any-other-wizard-ui-steps-blur-copy-behavior-warning-text-or-the-existing-linkmutation-call-flow",
      "title": "Update `frontend/src/components/settings/ConnectBotWizardModal.tsx` so `handleGenerateKeypair` no longer uses Web Crypto Ed25519 and instead generates the Ed25519 keypair using `@noble/ed25519` exactly as follows: `import * as ed from '@noble/ed25519'; const privateKey = ed.utils.randomPrivateKey(); const publicKey = await ed.getPublicKeyAsync(privateKey);`. Display the private key as a hex string (BOT_PRIVATE_KEY) and keep the public key as a `Uint8Array` for sending to the backend. If `@noble/ed25519` is not available at runtime/build-time, fall back to generating a 32-byte random seed and produce a placeholder derived public key, including the exact comment: `// TODO: Replace with real Ed25519 derivation in production.`. Do not change any other wizard UI steps, blur/copy behavior, warning text, or the existing `linkMutation` call flow.",
      "reqIds": [
        "REQ-41"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Wire existing backend/stripe/stripe.mo module into actor public interface: add stable stripeConfig, admin setStripeConfig, query isStripeConfigured, user-auth createCheckoutSession/update and getStripeSessionStatus/query; re-export ShoppingItem and StripeSessionStatus types; do not modify stripe.mo or existing endpoints/types.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-2",
      "summary": "Wire the existing Stripe module into the backend actor public interface without modifying the Stripe module file. Import the Stripe module from `backend/stripe/stripe.mo` and use its existing `createCheckoutSession` and `getSessionStatus` functions from the actor.",
      "reqIds": [
        "REQ-1"
      ],
      "status": "pending"
    },
    {
      "id": "AR-3",
      "summary": "Add stable Stripe configuration storage and admin/user-gated Stripe endpoints to `backend/main.mo`:\n- Add stable variable `stripeConfig : ?StripeConfiguration` (type imported from the Stripe module).\n- Add `isStripeConfigured` (query): returns `Bool` indicating whether `stripeConfig` is non-null.\n- Add `setStripeConfig` (update, admin only): input `secretKey : Text`, `allowedCountries : [Text]`; store as `StripeConfiguration`; return `#ok(\"Configured\")`.\n- Add `createCheckoutSession` (update, authenticated user required): input `items : [ShoppingItem]`, `successUrl : Text`, `cancelUrl : Text`; if `stripeConfig` is null return `#err` with message `\"Stripe not configured\"`; otherwise call `Stripe.createCheckoutSession` with (config, caller, items, URLs, and the existing actor `transform` function) and return the resulting `Text` (JSON string).\n- Add `getStripeSessionStatus` (query, authenticated user required): input `sessionId : Text`; if `stripeConfig` is null return an error; otherwise call `Stripe.getSessionStatus` with (config, sessionId, and the existing actor `transform` function) and return the `StripeSessionStatus` result.",
      "reqIds": [
        "REQ-2"
      ],
      "status": "pending"
    },
    {
      "id": "AR-4",
      "summary": "Import `ShoppingItem` and `StripeSessionStatus` types from `backend/stripe/stripe.mo` and re-export them as public actor types so the frontend can use them. Ensure any necessary upgrade/migration logic preserves existing stable state and initializes the newly added `stripeConfig` stable field safely during upgrades.",
      "reqIds": [
        "REQ-3"
      ],
      "status": "pending"
    },
    {
      "id": "AR-5",
      "summary": "Fix legacy getHeartbeatData in backend/main.mo: replace hardcoded values by looking up caller BotProfile (botProfiles) and UserProfile (license info), compute cycles using Runtime.cyclesBalance() or ExperimentalCycles.balance() (else 0 with TODO), set botStatus (#ACTIVE if botProfile exists and uplinkStatus true else #SUSPENDED), set lastHeartbeatAt (botProfile.lastHeartbeat else 0), set verifiedLicense by checking active license in licenses map via user accountId, and add backward-compatible optional field cyclesWarning:?Text when cycles < 500_000_000_000; do not modify other endpoints.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-6",
      "summary": "Fix the legacy `getHeartbeatData` implementation in `backend/main.mo` to return real data instead of hardcoded placeholders, while keeping the same public method name and overall single-actor architecture intact.",
      "reqIds": [
        "REQ-4"
      ],
      "status": "pending"
    },
    {
      "id": "AR-7",
      "summary": "Extend the backend `HeartbeatData` type to include a backward-compatible optional `cyclesWarning : ?Text` field and populate it when the canister cycle balance is below 500_000_000_000.",
      "reqIds": [
        "REQ-5"
      ],
      "status": "pending"
    },
    {
      "id": "AR-8",
      "summary": "Wire the existing Stripe module (`backend/stripe/stripe.mo`) into the main actor public interface in `backend/main.mo` without modifying `backend/stripe/stripe.mo`, including stable storage for Stripe configuration and the required admin/user-gated endpoints.",
      "reqIds": [
        "REQ-6"
      ],
      "status": "pending"
    },
    {
      "id": "AR-9",
      "summary": "Ensure upgrades preserve existing canister state while safely introducing the new stable `stripeConfig` field (adding conditional migration logic only if required by the current project’s upgrade policy).",
      "reqIds": [
        "REQ-7"
      ],
      "status": "pending"
    },
    {
      "id": "AR-10",
      "summary": "Update `backend/main.mo` `getHeartbeatData` so the `cycles` value no longer uses any unavailable cycles balance API: attempt to use `ExperimentalCycles.balance()` for the canister cycle balance; if `ExperimentalCycles.balance()` is not available in this Motoko version, set `cycles` to `0` and add the exact comment `// TODO: Replace with actual cycle balance API when available.`. Do not change any other logic, types, fields, access control, or behavior in `getHeartbeatData` beyond this `cycles` retrieval adjustment.",
      "reqIds": [
        "REQ-9"
      ],
      "status": "pending"
    },
    {
      "id": "AR-11",
      "summary": "Implement `check_uplink` fixes in `backend/main.mo`:\n- Add stable `botIdIndex : Map<Text, Principal>` mapping `bot_id` to owner Principal.\n- Update `saveCallerUserProfile` to maintain `botIdIndex` after storing the profile: if new `bot_id` exists, remove old mapping when `bot_id` changed, then add `newBotId -> caller`.\n- Replace `check_uplink` to use `botIdIndex.get(bot_id)` (trap with `\"Unknown bot ID: no user registered with this bot_id\"` if null), then fetch `BotProfile` (trap `\"No bot profile found for this bot ID\"` if null), require `publicKey` (trap `\"Bot public key not registered. Complete the Connect Bot wizard first.\"` if null), validate `signature.size() > 0` else trap `\"Missing signature: signature blob cannot be empty. The Python bot must sign requests with its private key.\"`, add TODO `// TODO Phase 2: Implement full Ed25519 signature verification`, and return `#EXECUTE` if `uplinkStatus` else `#STANDBY`.\n- Do not modify any other endpoints.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-12",
      "summary": "In `backend/main.mo`, add a new stable map `botIdIndex : Map<Text, Principal>` that maps `bot_id` (Text) to the owning user Principal.",
      "reqIds": [
        "REQ-10"
      ],
      "status": "pending"
    },
    {
      "id": "AR-13",
      "summary": "In `backend/main.mo` `saveCallerUserProfile`, after storing the incoming profile, update `botIdIndex` when `profile.bot_id` is non-null: (1) if a previous profile existed for the caller and had a different `bot_id`, remove the old `bot_id` entry from `botIdIndex`; (2) add/update the mapping `new bot_id -> caller` in `botIdIndex`.",
      "reqIds": [
        "REQ-11"
      ],
      "status": "pending"
    },
    {
      "id": "AR-14",
      "summary": "Replace the `check_uplink` implementation in `backend/main.mo` to: (1) resolve the owner Principal via `botIdIndex.get(bot_id)`; if null, `Runtime.trap(\"Unknown bot ID: no user registered with this bot_id\")`; (2) fetch the owner’s `BotProfile`; if null, `Runtime.trap(\"No bot profile found for this bot ID\")`; (3) require `publicKey` exists; if null, `Runtime.trap(\"Bot public key not registered. Complete the Connect Bot wizard first.\")`; (4) validate `signature` is non-empty; if `signature.size() == 0`, `Runtime.trap(\"Missing signature: signature blob cannot be empty. The Python bot must sign requests with its private key.\")`; (5) add the comment `// TODO Phase 2: Implement full Ed25519 signature verification`; (6) return `#EXECUTE` if `uplinkStatus` is true, else `#STANDBY`.",
      "reqIds": [
        "REQ-12"
      ],
      "status": "pending"
    },
    {
      "id": "AR-15",
      "summary": "Fix `fetchSignals` in `backend/main.mo` to return the HTTP response body instead of `#ok([])`: change `SignalFetchResult` to `#ok : { statusCode : Nat; body : Text; timestamp : Int } | #err : BotError`. On successful outcall (no trap), return `#ok({ statusCode = 200; body = response; timestamp = Time.now() })` while still updating `lastHeartbeat` as currently implemented; keep existing `#err` returns on errors. Add an audit entry: `\"Signals fetched: \" # (if response.size() > 100 then first 100 chars else full response)`. Update public type definitions so frontend can consume the new result shape. Do not modify any other endpoints.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-16",
      "summary": "Update the backend public type `SignalFetchResult` in `backend/main.mo` to return a structured HTTP response payload instead of `[Signal]`, changing it to: `#ok : { statusCode : Nat; body : Text; timestamp : Int } | #err : BotError` (keeping the existing `BotError` variants unchanged).",
      "reqIds": [
        "REQ-13"
      ],
      "status": "pending"
    },
    {
      "id": "AR-17",
      "summary": "In `backend/main.mo`, update `fetchSignals` so that after a successful HTTP outcall (i.e., after `response = await OutCall.httpGetRequest(...)` completes without trapping), the function returns `#ok({ statusCode = 200; body = response; timestamp = Time.now() })` instead of `#ok([])`.",
      "reqIds": [
        "REQ-14"
      ],
      "status": "pending"
    },
    {
      "id": "AR-18",
      "summary": "Keep the existing `fetchSignals` error behavior in `backend/main.mo` unchanged: on authorization failure, missing bot profile, missing bot URL, invalid URL, or any existing error case, continue returning the same `#err` variants and messages as currently implemented.",
      "reqIds": [
        "REQ-15"
      ],
      "status": "pending"
    },
    {
      "id": "AR-19",
      "summary": "In `backend/main.mo`, ensure `fetchSignals` still updates the caller’s bot profile heartbeat timestamp exactly as it currently does (i.e., set `lastHeartbeat = Time.now()` in the stored `BotProfile`) on the successful outcall path.",
      "reqIds": [
        "REQ-16"
      ],
      "status": "pending"
    },
    {
      "id": "AR-20",
      "summary": "In `backend/main.mo`, add an audit log entry on the successful outcall path of `fetchSignals` whose details include the first 100 characters of the HTTP response body (or the full body if it is <= 100 characters), using the exact prefix format: `\"Signals fetched: \" # <snippet>`.",
      "reqIds": [
        "REQ-17"
      ],
      "status": "pending"
    },
    {
      "id": "AR-21",
      "summary": "Regenerate the frontend Candid-derived TypeScript interface/declaration files so the generated `SignalFetchResult` and `actor.fetchSignals()` types match the updated backend result shape (`{ ok: { statusCode; body; timestamp } } | { err: BotError }`).",
      "reqIds": [
        "REQ-19"
      ],
      "status": "in_progress"
    },
    {
      "id": "AR-22",
      "summary": "Regenerate or update the frontend’s Candid-derived TypeScript interface/declaration files used by the frontend actor (the `backendInterface` imported from `../backend`) so TypeScript correctly reflects the updated backend `SignalFetchResult` shape returned by `actor.fetchSignals()` (success payload includes `statusCode`, `body`, and `timestamp`).",
      "reqIds": [
        "REQ-20"
      ],
      "status": "pending"
    },
    {
      "id": "AR-23",
      "summary": "Fix Ed25519 key generation in Connect Bot Wizard (frontend/src/components/settings/ConnectBotWizardModal.tsx): replace unrelated random buffers with a real Ed25519 keypair using TweetNaCl (preferred) or fallback to @noble/ed25519 / WebCrypto Ed25519 if dependency install isn’t possible; display secretKey as hex (BOT_PRIVATE_KEY) and keep publicKey as Uint8Array for backend; do not modify any other files/components.",
      "reqIds": [
        "REQ-9"
      ],
      "status": "pending"
    },
    {
      "id": "AR-26",
      "summary": "If TweetNaCl is not currently present in the project dependencies, add it as a frontend dependency so `import nacl from 'tweetnacl'` compiles and runs; do not introduce any other dependency changes beyond what is required to support the Ed25519 keypair generation fix.",
      "reqIds": [
        "REQ-23"
      ],
      "status": "pending"
    },
    {
      "id": "AR-27",
      "summary": "Update Connect Bot Wizard `handleGenerateKeypair` implementation to use `@noble/ed25519` as the primary Ed25519 keypair generator (since TweetNaCl and WebCrypto Ed25519 are unavailable): `import * as ed from '@noble/ed25519'; const privateKey = ed.utils.randomPrivateKey(); const publicKey = await ed.getPublicKeyAsync(privateKey);`. If `@noble/ed25519` is also unavailable, fall back to generating a 32-byte random seed and set/derive a placeholder public key with the comment `// TODO: Replace with real Ed25519 derivation in production.`. Do not change anything else in the component.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-28",
      "summary": "Update `backend/main.mo` `getHeartbeatData(accountId)` to return real, non-hardcoded data derived from existing canister state while preserving the same public method name and access control.\n- Look up the caller’s `BotProfile` from `botProfiles` and use it to set `botStatus` (`#ACTIVE` only when a bot profile exists and `uplinkStatus == true`, otherwise `#SUSPENDED`) and `lastHeartbeatAt` (use `botProfile.lastHeartbeat` when present, otherwise `0`).\n- Determine `verifiedLicense` by looking up the caller’s `UserProfile` in `userProfiles`, reading `accountId`, then checking `licenses.get(accountId)` and returning `true` only if a license exists and `active == true`.\n- Keep `cycles` retrieval constrained to the current Motoko environment: if a cycle-balance API is not available, keep `cycles = 0` and include the exact comment `// TODO: Replace with actual cycle balance API when available.` (do not introduce other cycle APIs).\n- Populate `cyclesWarning : ?Text` when `cycles < 500_000_000_000`, otherwise `null`.",
      "reqIds": [
        "REQ-25"
      ],
      "status": "pending"
    },
    {
      "id": "AR-29",
      "summary": "Harden `backend/main.mo` `fetchSignals()` success-path audit logging so it never traps when creating the response snippet:\n- Keep returning the structured success result shape `#ok({ statusCode : Nat; body : Text; timestamp : Int })`.\n- When creating the audit-log snippet, include the full response body if `response.size() <= 100`, otherwise include only the first 100 characters.\n- Ensure any slicing/substring logic uses bounds-safe end indices (no out-of-range slicing).",
      "reqIds": [
        "REQ-26"
      ],
      "status": "pending"
    },
    {
      "id": "AR-30",
      "summary": "Fix `backend/main.mo` `saveCallerUserProfile` bot-id indexing logic so `botIdIndex` correctly removes the previous `bot_id` mapping when the caller changes their `bot_id`.\n- Capture the prior profile (if any) before overwriting `userProfiles`.\n- After saving the new profile, if the old profile had a different `bot_id`, remove the old mapping from `botIdIndex`.\n- If the new profile has `bot_id = ?Text`, set/overwrite `botIdIndex[newBotId] = caller`.\n- If the new profile has `bot_id = null`, do not add a new index entry (and remove the old one if it existed).",
      "reqIds": [
        "REQ-27"
      ],
      "status": "pending"
    },
    {
      "id": "AR-31",
      "summary": "Frontend routing + Settings merge refactor:\n- Refactor `AppShell.tsx` to replace useState-based screen toggling with a proper route system (state variable or simple hash routing) supporting: 'dashboard' | 'trades' | 'admin' | 'overview' | 'settings' | 'payment-success' | 'payment-failure'.\n- Update `DashboardHeader` navigation to include links: Dashboard (default), Trades, Overview, Settings, and Admin (admin-only via `useIsCallerAdmin`).\n- Render screens by route: dashboard→DashboardScreen, trades→TradesScreen, overview→OverviewScreen, settings→merged SettingsScreen, admin→AdminScreen (admin-only), payment-success→PaymentSuccessScreen, payment-failure→PaymentFailureScreen.\n- Merge `SettingsScreen.tsx` + `SettingsRouteScreen.tsx` into one Settings screen with 4 tabs: Profile (ProfileSettingsForm), Connect Bot (ConnectBotPanel), Subscription (SubscriptionPanel, always visible), Admin Tiers (AdminTierManagementPanel, admin-only).\n- Import all screens and `useIsCallerAdmin` for admin checks.\n- Delete `SettingsRouteScreen.tsx` after merging.\n- Do not modify internals of DashboardScreen/TradesScreen/other screens; only AppShell routing + Settings tabs/composition.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-32",
      "summary": "Remove support-related text strings from the Subscription UI/tab: delete the phrases \"Community support\", \"Priority Support\", and \"Dedicated Support\" so they no longer appear anywhere in Subscription.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-33",
      "summary": "Make the Subscription tab responsive and interactive by wiring existing hooks into `SubscriptionPanel.tsx`: use `useListTiers` to load tiers + determine/highlight the user’s active tier, and use `useStripeCheckout` so clicking a tier card initiates checkout immediately. Ensure layout is optimized for all screen sizes with tier cards stacking vertically on small screens.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-34",
      "summary": "Optimize the entire app UI for ALL screen sizes (responsive layout across all screens/components), beyond just the Subscription tab.",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-35",
      "summary": "Update `frontend/src/components/settings/SubscriptionPanel.tsx` only: replace hardcoded tiers with `useListTiers()` data (show loading spinner; if error/empty, fall back to existing hardcoded tiers). Add per-paid-tier \"Subscribe\" button that calls `createCheckoutSession` (from `useCreateCheckoutSession`) with a `ShoppingItem` built from tier name/description/priceInCents and quantity=1; on success redirect `window.location.href = session.url`; show button spinner while pending. Free tier shows \"Current Plan\" instead of a button. Use `useIsStripeConfigured` to disable Subscribe buttons when false and show message: \"Payment processing is being configured. Check back soon.\" Preserve glassmorphism styling and \"Popular\" badge. Do not modify any other files.",
      "reqIds": [],
      "status": "pending"
    }
  ],
  "gameProjectType": "none",
  "projectName": "Expose Stripe checkout/session APIs via backend actor (stable config, admin setup, user-auth calls)",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}